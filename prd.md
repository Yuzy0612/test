# 乍得PSA二三批油田虚拟流量计量系统（VFM）PRD

## 1. 文档信息
- 文档版本：v1.0
- 编写日期：2026-04-27
- 适用范围：Birrea、Casava、Parkia、Moul、Raphia 5个油田，首期50口井，支持扩展至150口井
- 项目目标：建设分钟级在线三相计量系统，支持生产优化、计量核算、区块管理与报表决策

## 2. 项目背景与目标

### 2.1 项目背景
乍得PSA二三批油田当前井口生产监测依赖离散测试与人工统计，存在以下问题：
- 单井油、气、水三相产量难以分钟级在线获取，响应滞后
- ESP/PCP/ESPCP井型工况差异明显，统一计量模型难以兼顾
- 区块回配与报表生成依赖人工处理，效率和一致性不足
- 缺少统一模型治理与标定数据资产管理能力

### 2.2 建设目标
- 基于差压法机理模型与LSTM耦合算法，实现单井三相产量分钟级在线计量
- 计量误差控制在 ±15% 以内（以标定/测试数据为基准）
- 支持ESP、PCP、ESPCP三种采油方式的建模与运行
- 打通梦想云门户（SSO与权限集成）和PI实时数据库（单向同步）
- 提供中英文双语界面和双机热备部署能力（主备切换 < 30秒）
- 支持50口井首期建设并平滑扩容至150口井

### 2.3 成功指标（KPI）
- **计量准确性**：月度有效样本中，单井三相产量误差 ≤ ±15% 的比例不低于 90%
- **时效性**：从数据到达至结果可见，95分位延迟 ≤ 120秒
- **可用性**：系统月度可用性 ≥ 99.5%，主备切换时间 < 30秒
- **扩展性**：新增单井接入配置完成时间 ≤ 15分钟（不含现场硬件）
- **报表效率**：日报自动生成成功率 ≥ 99%

## 3. 范围定义

### 3.1 In Scope
- 单井分钟级油/气/水三相产量在线计算
- 差压机理模型与LSTM模型耦合推理
- 模型训练、验证、发布、回滚流程管理
- 标定数据管理与质量校验
- 区块产量聚合与回配
- 历史趋势分析与多维对比
- 报告自动生成与导出
- 统一登录与权限映射
- 与PI实时数据库单向数据同步
- 中英文双语支持与主备高可用部署

### 3.2 Out of Scope（本期不含）
- 双向写回PI或对PI数据结构改造
- 现场仪表采购、安装与通讯改造施工
- 非油气水三相（如砂含量）专用计量模型
- 跨公司多租户运营结算系统

## 4. 用户角色与权限
- **生产工程师**：查看实时产量、趋势分析、井状态、异常告警
- **计量工程师**：标定数据维护、模型校验、误差分析、模型发布建议
- **区块管理者**：区块回配规则配置、产量对账、汇总报表
- **管理层用户**：查看KPI看板、日报/月报、生产对比
- **系统管理员**：用户角色映射、字典配置、接口监控、HA切换演练

> 权限原则：通过梦想云SSO鉴权后，按角色-资源-动作（RBAC）进行细粒度授权。

## 5. 业务流程概览
1. 采集层接入实时数据（压力、温度、差压、电参等）
2. 数据质量引擎进行校验、补点、时间对齐
3. 机理模型与LSTM模型耦合计算三相瞬时产量
4. 结果入库并用于趋势、回配、报表
5. 关键结果按约定格式单向同步到PI
6. 用户通过梦想云单点登录进入系统按角色使用功能

## 6. 功能需求

### FR-01 实时计量引擎
**目标**：提供单井分钟级油/气/水三相产量在线计量。  
**输入**：井口压力、温度、差压、ESP/PCP/ESPCP电参、运行状态等。  
**输出**：单井分钟产量（油/气/水）、当日累计、状态标签。  
**规则**：
- 采样周期：1分钟（支持5秒至1分钟原始数据聚合）
- 对迟到数据（默认10分钟内）触发自动重算
- 缺失值处理：按规则插值或标记无效并告警
- 结果状态：`valid`、`estimated`、`invalid`

**验收标准**：
- 50口井并发运行时，计量任务成功率 ≥ 99%
- 95分位单井计算耗时 ≤ 5秒

### FR-02 差压机理模型与LSTM耦合
**目标**：增强复杂工况下计量鲁棒性与精度。  
**功能点**：
- 机理模型参数配置（按井/井型）
- LSTM训练任务管理（数据集、特征、窗口、超参数）
- 耦合策略配置（加权融合、场景切换、异常兜底）
- 模型版本管理（草稿、待发布、已发布、已回退）
- 一键回滚到上一稳定版本

**验收标准**：
- 任一上线模型均可追溯训练数据集与参数快照
- 误差评估页面可按井/井型输出MAE、MAPE、RMSE

### FR-03 采油方式适配（ESP/PCP/ESPCP）
**目标**：适配不同举升方式的输入参数与计算策略。  
**功能点**：
- 井型模板：默认参数、约束范围、特征映射
- 井型切换：支持在配置界面动态变更并重载策略
- 井型校验：配置变更时自动检查必要字段

**验收标准**：
- 三种井型均支持独立建模、独立配置、独立统计

### FR-04 标定数据管理
**目标**：建立可信标定数据资产，用于模型校验与修正。  
**功能点**：
- 标定数据导入（手工录入、批量上传）
- 数据质检（时间完整性、范围合理性、重复检测）
- 标定版本管理（生效时间窗、版本说明、审批记录）
- 标定-模型关联（可追溯某模型使用的标定版本）

**验收标准**：
- 标定记录支持按井、时间、版本检索
- 数据质检规则可配置并产出错误明细

### FR-05 区块产量回配
**目标**：将单井产量按业务规则回配到区块/层系维度。  
**功能点**：
- 区块映射关系维护（井-区块-层系）
- 回配规则配置（权重、优先级、时间生效）
- 回配结果校验（总量守恒、异常偏差提示）
- 历史版本对比（回配前后差异）

**验收标准**：
- 回配结果可追溯规则版本与执行时间
- 支持分钟、小时、日粒度汇总

### FR-06 历史趋势分析
**目标**：支持生产诊断与长期优化分析。  
**功能点**：
- 多维筛选（油田/区块/井/井型/时间）
- 曲线展示（油气水产量、含水率、气油比、关键输入参数）
- 对比分析（多井对比、时段对比、模型版本对比）
- 数据导出（CSV/Excel）

**验收标准**：
- 1年历史数据查询响应时间（常规条件）≤ 5秒

### FR-07 产量报告生成
**目标**：自动形成标准化生产报告。  
**功能点**：
- 报告模板管理（日报/周报/月报）
- 定时任务生成与手动触发
- 双语报告输出（中文/英文）
- 导出格式：PDF、Excel

**验收标准**：
- 定时生成失败可自动重试并通知管理员

### FR-08 系统集成与权限
**目标**：完成与梦想云和PI的标准集成。  
**功能点**：
- 梦想云SSO登录与Token校验
- 外部角色到本系统角色映射
- PI单向同步（仅写指定标签/测点）
- 同步监控与失败补偿重传

**验收标准**：
- 未授权用户无法访问受限接口
- PI同步失败重试机制可配置（次数/间隔）

### FR-09 动态扩展能力
**目标**：支持后续油井快速接入，平滑扩容到150口井。  
**功能点**：
- 批量新增井基础信息与模板配置
- 接入检查向导（数据源、字段、单位、时间戳）
- 容量监控（计算队列、延迟、资源利用率）

**验收标准**：
- 150口井规模下核心SLA（准确性、时效性）不降低到目标阈值以下

## 7. 非功能需求

### 7.1 性能
- 50口井：分钟级任务准时计算率 ≥ 99%
- 150口井：系统可稳定扩展，95分位端到端延迟 ≤ 180秒
- API吞吐：核心查询接口支持 ≥ 300 QPS（可水平扩展）

### 7.2 高可用与容灾
- 双机热备部署（主备）
- 主备切换时间 < 30秒
- 关键服务健康检查周期 ≤ 5秒
- 数据库采用主从或等价高可用方案，RPO ≤ 1分钟，RTO ≤ 10分钟

### 7.3 可靠性与数据质量
- 采集数据完整性检查、时间戳对齐、异常值识别
- 计算任务具备幂等性与断点续算能力
- 所有关键业务操作可审计（模型发布、规则变更、权限变更）

### 7.4 安全
- 统一SSO接入，JWT/OAuth2兼容
- API全链路TLS
- 细粒度RBAC权限控制
- 关键数据（口令、密钥）加密存储
- 审计日志保留不少于365天

### 7.5 可观测性
- 指标监控：延迟、错误率、同步成功率、模型精度
- 日志分级与可检索
- 告警分级（P1/P2/P3）与通知策略（邮件/IM）

### 7.6 国际化
- UI中英文双语，支持运行时切换
- 报表与字典项支持双语模板
- 时间、数字、单位显示遵循区域化配置

### 7.7 可维护与可扩展
- 模块化架构，采集、计算、服务、报表解耦
- 井配置模板化，新增井无需改代码
- 接口向后兼容（`/api/v1`版本管理）

## 8. 数据与模型要求

### 8.1 输入数据项（最小集合）
- 井标识：wellId、field、block、liftType
- 压力类：井口压力、套压、油压（按可用性）
- 温度类：井口温度、流体温度
- 差压类：节流差压/计量段差压
- 电参类：电流、电压、功率、频率（ESP/ESPCP重点）
- 运行状态：开井/停井、故障码、工况标签

### 8.2 模型治理
- 模型生命周期：开发 -> 验证 -> 发布 -> 监控 -> 回滚
- 精度监控窗口：日/周/月
- 漂移检测：输入分布漂移与误差漂移
- 回滚条件：连续N个窗口误差超过阈值自动触发预警

## 9. 用户界面草图（文字描述）

### 9.1 页面A：实时监控看板
- 顶部：油田/区块/井筛选器 + 时间范围 + 语言切换
- 左侧：井列表（状态颜色：正常/预警/异常）
- 中央：单井实时卡片（油/气/水瞬时值、累计值、误差状态）
- 右侧：告警面板（数据缺失、模型异常、同步失败）
- 底部：分钟级趋势小图（最近2小时）

### 9.2 页面B：单井详情与模型状态
- 头部：井基础信息（井型、采油方式、运行状态）
- 选项卡1「实时曲线」：输入参数与产量曲线联动
- 选项卡2「模型版本」：当前版本、发布时间、发布人、回滚按钮
- 选项卡3「误差分析」：与标定值对比图、统计指标
- 关键交互：切换模型版本前弹出影响提示与确认框

### 9.3 页面C：标定数据管理
- 左栏：按井/时间筛选
- 主区：标定记录表（时间、油气水、来源、质量状态）
- 顶部操作：导入、质检、提交审批、生效设置
- 明细抽屉：显示校验错误和修复建议

### 9.4 页面D：区块回配中心
- 顶部：区块选择、时间粒度选择
- 左侧：回配规则配置区（权重、优先级、生效时间）
- 右侧：回配结果预览（回配前后差值、总量守恒提示）
- 底部：执行日志与历史版本对比入口

### 9.5 页面E：历史趋势分析
- 顶部：多维筛选（油田/区块/井/井型/模型版本）
- 中部：主图表区（可叠加多曲线）
- 右侧：统计摘要（均值、峰值、波动率）
- 导出按钮：导出当前筛选结果

### 9.6 页面F：报表中心
- 报表目录：日报/周报/月报/自定义
- 模板设置：中英文模板切换、字段勾选
- 任务区：定时任务列表、执行状态、失败重试
- 预览区：报告内容预览与一键导出

### 9.7 页面G：系统管理与集成监控
- SSO映射管理：外部角色 -> 本系统角色
- PI同步监控：同步任务状态、失败明细、重试操作
- HA状态：主备节点健康、切换按钮（仅管理员）
- 审计日志：按用户/操作类型/时间检索

## 10. API接口定义（REST）

### 10.1 通用约定
- 基础路径：`/api/v1`
- 鉴权：`Authorization: Bearer <token>`
- 数据格式：`application/json`
- 通用响应结构：

```json
{
  "code": 0,
  "message": "ok",
  "data": {}
}
```

- 常见错误码：
  - `1001` 参数校验失败
  - `1002` 未认证或令牌失效
  - `1003` 无权限访问
  - `2001` 数据源不可用
  - `3001` 模型计算失败
  - `4001` 外部系统同步失败

### 10.2 井管理
#### 1) 获取井列表
- 路径：`GET /api/v1/wells`
- 查询参数：
  - `field` (string, optional)
  - `block` (string, optional)
  - `liftType` (enum: ESP/PCP/ESPCP, optional)
  - `page` (int, default 1)
  - `pageSize` (int, default 20)

**返回示例**
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": [
      {
        "wellId": "BIR-001",
        "field": "Birrea",
        "block": "B1",
        "liftType": "ESP",
        "status": "running"
      }
    ],
    "total": 50
  }
}
```

#### 2) 新增井（批量）
- 路径：`POST /api/v1/wells/batch`
- 请求体参数：
  - `wells` (array, required)
    - `wellId` (string)
    - `field` (string)
    - `block` (string)
    - `liftType` (ESP/PCP/ESPCP)
    - `templateId` (string)

**返回示例**
```json
{
  "code": 0,
  "message": "created",
  "data": {
    "created": 10,
    "failed": 0
  }
}
```

### 10.3 实时计量
#### 3) 获取单井实时三相产量
- 路径：`GET /api/v1/vfm/wells/{wellId}/realtime`
- 路径参数：`wellId` (string, required)

**返回示例**
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "wellId": "BIR-001",
    "timestamp": "2026-04-27T09:10:00+08:00",
    "oilRate": 52.3,
    "gasRate": 1120.5,
    "waterRate": 38.7,
    "qualityFlag": "valid",
    "modelVersion": "vfm-esp-20260420-01"
  }
}
```

#### 4) 获取多井实时产量汇总
- 路径：`POST /api/v1/vfm/realtime/query`
- 请求体参数：
  - `wellIds` (array[string], required)
  - `time` (string, optional, ISO8601；默认当前分钟)

**返回示例**
```json
{
  "code": 0,
  "message": "ok",
  "data": [
    {
      "wellId": "BIR-001",
      "oilRate": 52.3,
      "gasRate": 1120.5,
      "waterRate": 38.7
    }
  ]
}
```

### 10.4 标定与模型
#### 5) 上传标定数据
- 路径：`POST /api/v1/calibration/import`
- 请求体参数：
  - `wellId` (string, required)
  - `records` (array, required)
    - `timestamp` (string)
    - `oil` (number)
    - `gas` (number)
    - `water` (number)
    - `source` (string)

**返回示例**
```json
{
  "code": 0,
  "message": "imported",
  "data": {
    "accepted": 120,
    "rejected": 3,
    "errors": [
      "record[7] duplicated timestamp"
    ]
  }
}
```

#### 6) 发布模型版本
- 路径：`POST /api/v1/models/{modelId}/publish`
- 路径参数：`modelId` (string, required)
- 请求体参数：
  - `effectiveFrom` (string, required)
  - `comment` (string, optional)

**返回示例**
```json
{
  "code": 0,
  "message": "published",
  "data": {
    "modelId": "vfm-esp-20260420-01",
    "status": "online",
    "effectiveFrom": "2026-04-27T10:00:00+08:00"
  }
}
```

#### 7) 回滚模型版本
- 路径：`POST /api/v1/models/{modelId}/rollback`
- 请求体参数：
  - `targetVersion` (string, required)
  - `reason` (string, required)

**返回示例**
```json
{
  "code": 0,
  "message": "rollback started",
  "data": {
    "currentVersion": "vfm-esp-20260420-01",
    "targetVersion": "vfm-esp-20260410-03"
  }
}
```

### 10.5 区块回配
#### 8) 执行区块回配
- 路径：`POST /api/v1/allocation/execute`
- 请求体参数：
  - `blockId` (string, required)
  - `startTime` (string, required)
  - `endTime` (string, required)
  - `ruleVersion` (string, required)
  - `granularity` (enum: minute/hour/day, required)

**返回示例**
```json
{
  "code": 0,
  "message": "allocation completed",
  "data": {
    "taskId": "alloc-20260427-001",
    "deviation": 0.003,
    "conservationCheck": true
  }
}
```

### 10.6 趋势与报表
#### 9) 查询历史趋势
- 路径：`GET /api/v1/trends`
- 查询参数：
  - `wellId` (string, optional)
  - `blockId` (string, optional)
  - `metrics` (string, required, 逗号分隔)
  - `startTime` (string, required)
  - `endTime` (string, required)
  - `interval` (enum: minute/hour/day, required)

**返回示例**
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "series": [
      {
        "metric": "oilRate",
        "points": [
          ["2026-04-27T08:00:00+08:00", 51.2],
          ["2026-04-27T08:01:00+08:00", 52.1]
        ]
      }
    ]
  }
}
```

#### 10) 生成产量报告
- 路径：`POST /api/v1/reports/generate`
- 请求体参数：
  - `reportType` (enum: daily/weekly/monthly/custom, required)
  - `lang` (enum: zh/en, required)
  - `scope` (object, required)
  - `format` (enum: pdf/xlsx, required)

**返回示例**
```json
{
  "code": 0,
  "message": "report queued",
  "data": {
    "reportId": "rpt-20260427-088",
    "status": "processing"
  }
}
```

### 10.7 集成与运维
#### 11) 查询PI同步状态
- 路径：`GET /api/v1/integrations/pi/sync-status`
- 查询参数：
  - `startTime` (string, optional)
  - `endTime` (string, optional)

**返回示例**
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "successRate": 0.992,
    "failedCount": 12,
    "lastSyncTime": "2026-04-27T09:09:00+08:00"
  }
}
```

#### 12) 查询HA节点状态
- 路径：`GET /api/v1/system/ha/status`

**返回示例**
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "primary": "node-a",
    "standby": "node-b",
    "primaryHealth": "healthy",
    "standbyHealth": "healthy",
    "lastFailover": "2026-04-10T02:15:00+08:00"
  }
}
```

## 11. 验收标准

### 11.1 功能验收
- 覆盖50口井在线计量，支持三种采油方式
- 支持标定管理、区块回配、趋势分析、报告生成
- 与梦想云实现SSO与权限映射
- PI单向同步稳定运行并可监控追踪

### 11.2 性能与稳定性验收
- 计量误差目标：≤ ±15%
- 分钟级结果可见，95分位延迟满足要求
- 双机热备切换 < 30秒
- 在150口井压测下系统保持可用与可观测

### 11.3 安全与合规验收
- 鉴权鉴权链路完整，越权访问被阻断
- 关键操作审计可追溯
- 传输与存储加密策略可验证

## 12. 里程碑建议
- M1：需求冻结与数据梳理（2周）
- M2：核心计量引擎与基础页面（4周）
- M3：模型治理、回配、报表与集成联调（4周）
- M4：HA演练、性能压测与试运行（2周）
- M5：首期上线（50井）与扩容准备（2周）

## 13. 依赖与约束
- 依赖现场数据质量与PI标签完整性
- 依赖梦想云SSO对接环境与权限目录
- 依赖业务方提供足够标定数据用于模型训练/验证
- 受网络稳定性与跨系统接口SLA影响

## 14. PRD 自评

### 14.1 优点（3项）
1. **完整性高**：覆盖功能、非功能、UI、API、验收与里程碑，便于研发与实施协同。
2. **工程可落地**：对关键指标（精度、延迟、HA切换）给出了可验证目标，便于测试验收。
3. **扩展性明确**：针对50到150口井扩容、模型治理、集成解耦均提出了可执行设计原则。

### 14.2 可能风险/遗漏点（3项）
1. **标定数据不足风险**：若标定样本覆盖工况不足，可能无法稳定达到 ±15% 精度目标。
2. **外部系统依赖风险**：梦想云或PI接口策略变更可能导致联调周期拉长。
3. **现场工况突变风险**：井况变化和设备故障频发会导致模型漂移，需要更频繁重训与版本治理。
